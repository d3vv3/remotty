# Browser-facing broker URL. Use wss:// outside local development.
VITE_REMOTTY_URL=ws://localhost:8787/ws

# Broker listen port.
PORT=8787

# Keep these stable in production so Push subscriptions survive broker restarts.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com
