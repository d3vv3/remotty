import { Plugin } from "@opencode/plugin"

/** Server discovery entrypoint. The relay requires the richer local TUI context. */
export default Plugin.define({
  id: "remotty",
  setup: () => undefined,
})
